P = {
	LISTA_COMANDO -> COMANDO LISTA_COMANDO || VAZIO
	
	COMANDO -> <IDENTIFICADOR> ATRIBUIÇÃO || <OUT> EXIBICAO || <IN> ENTRADA|| <IF> CONDICIONAL 

	ATRIBUIÇÃO -> <IGUAL> E1  <PONTOVIRGULA>
	
	ENTRADA -> <IDENTIFICADOR> CONTROLE_ENTRADA 
	CONTROLE_ENTRADA -> <VIRGULA> ENTRADA || <PONTOVIRGULA> 
	
	CONDICIONAL -> <LPARENTES> E1 <RPARENTES>  <LCHAVES,> COMANDO <RCHAVES,> COND_ELSE
	COND_ELSE -> <ELSE,> <LCHAVES,> LISTA_COMANDO <RCHAVES,> || VAZIO
	
	EXIBIÇÃO -> E1 CONTROLE_EXIBICAO 
	CONTROLE_EXIBICAO -> <VIRGULA> EXIBICAO || <PONTOVIRGULA,> 
	
	E1 -> E2 <-> E1 || E2
	E2 -> E3 -> E2 || E3
	E3 -> E3 V E4 ||  E3 ^  E4 || E4
	E4 -> B' || B
 	B  -> VAR_BOL  || <LPARENTS,> E1 <RPARENTS,>
	
	VAR_BOL -> <IDENTIFICADOR> || <TRUE> || <FALSE>
	
	

	}

Simbolos da linguagem:

E = {
	IDENTIFICADOR,
	BOOLEANO,
	TRUE,
	FALSE,
	RESERVADO,
	IF,
	ELSE,
	IN,
	OUT,
	OPERADOR,
	OR,
	AND,
	NOT,
	OPERADORSE,
	OPERADORSSS,
	ATRIBUICAO,
	LPARENTS,
	RPARENTS,
	LCHAVES,
	RCHAVES,
	PONTOVIRGULA,
	VIRGULA,
	INICIO,

}

Conjunto de Simbolos da pilha:
PI = {
	VAZIO = 0,
	IDENTIFICADOR,
	BOOLEANO,
	IF,
	ELSE,
	IN,
	OUT,
	OPERADOR,
	ATRIBUICAO,
	LPARENTS,
	RPARENTS,
	LCHAVES,
	RCHAVES,
	PONTOVIRGULA,
	VIRGULA,
	BOOLVAR,
	BOOLCOND,
	CONDICIONAL,
	PARENTSIN,
	ENTRADA,
	INICIO,
}
